///
/// HybridChildrenContainerTestViewSpec.swift
/// This file was generated by nitrogen. DO NOT MODIFY THIS FILE.
/// https://github.com/margelo/nitro
/// Copyright © Marc Rousavy @ Margelo
///

import NitroModules
import UIKit

/// See ``HybridChildrenContainerTestViewSpec``
public protocol HybridChildrenContainerTestViewSpec_protocol: HybridObject, HybridView {
  // Properties
  /**
   * The ``UIView`` React children are mounted into.
   *
   * Defaults to ``view``. Override this when the children have to live inside
   * a sub-view - e.g. ``UIVisualEffectView/contentView``, which is where a
   * blur view expects its content. The sub-view has to cover the same area as
   * ``view``, otherwise React Native's layout lands in the wrong place.
   *
   * Like ``view``, this value should not change during the lifetime of this
   * ``HybridView``.
   */
  var childrenContainer: UIView { get }
  var isBlue: Bool { get set }

  // Methods
  func getNativeChildCount() throws -> Double
  func getViewChildCount() throws -> Double
}

public extension HybridChildrenContainerTestViewSpec_protocol {
  /// Default implementation of ``childrenContainer``
  var childrenContainer: UIView {
    return self.view
  }
  
  /// Default implementation of ``HybridObject.toString``
  func toString() -> String {
    return "[HybridObject ChildrenContainerTestView]"
  }
}

/// See ``HybridChildrenContainerTestViewSpec``
open class HybridChildrenContainerTestViewSpec_base {
  private weak var cxxWrapper: HybridChildrenContainerTestViewSpec_cxx? = nil
  public init() { }
  public func getCxxWrapper() -> HybridChildrenContainerTestViewSpec_cxx {
  #if DEBUG
    guard self is any HybridChildrenContainerTestViewSpec else {
      fatalError("`self` is not a `HybridChildrenContainerTestViewSpec`! Did you accidentally inherit from `HybridChildrenContainerTestViewSpec_base` instead of `HybridChildrenContainerTestViewSpec`?")
    }
  #endif
    if let cxxWrapper = self.cxxWrapper {
      return cxxWrapper
    } else {
      let cxxWrapper = HybridChildrenContainerTestViewSpec_cxx(self as! any HybridChildrenContainerTestViewSpec)
      self.cxxWrapper = cxxWrapper
      return cxxWrapper
    }
  }
}

/**
 * A Swift base-protocol representing the ChildrenContainerTestView HybridObject.
 * Implement this protocol to create Swift-based instances of ChildrenContainerTestView.
 * ```swift
 * class HybridChildrenContainerTestView : HybridChildrenContainerTestViewSpec {
 *   // ...
 * }
 * ```
 */
public typealias HybridChildrenContainerTestViewSpec = HybridChildrenContainerTestViewSpec_protocol & HybridChildrenContainerTestViewSpec_base
