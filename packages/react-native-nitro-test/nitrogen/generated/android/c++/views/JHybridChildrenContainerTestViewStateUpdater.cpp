///
/// JHybridChildrenContainerTestViewStateUpdater.cpp
/// This file was generated by nitrogen. DO NOT MODIFY THIS FILE.
/// https://github.com/margelo/nitro
/// Copyright © Marc Rousavy @ Margelo
///

#include "JHybridChildrenContainerTestViewStateUpdater.hpp"
#include "views/HybridChildrenContainerTestViewComponent.hpp"
#include <NitroModules/NitroDefines.hpp>
#include <react/fabric/StateWrapperImpl.h>

namespace margelo::nitro::test::views {

using namespace facebook;
using ConcreteStateData = react::ConcreteState<HybridChildrenContainerTestViewState>;

std::shared_ptr<const HybridChildrenContainerTestViewProps> JHybridChildrenContainerTestViewStateUpdater::getPropsFromStateWrapper(
    jni::alias_ref<JStateWrapper::javaobject> stateWrapper) {
  if (stateWrapper.get() == nullptr) {
    return nullptr;
  }
  // Get concrete StateWrapperImpl from passed StateWrapper interface object
  jobject rawStateWrapper = stateWrapper.get();
  if (!stateWrapper->isInstanceOf(react::StateWrapperImpl::javaClassStatic())) [[unlikely]] {
    throw std::runtime_error("StateWrapper is not a StateWrapperImpl");
  }
  auto stateWrapperImpl = jni::alias_ref<react::StateWrapperImpl::javaobject>{
    static_cast<react::StateWrapperImpl::javaobject>(rawStateWrapper)
  };
  std::shared_ptr<const react::State> state = stateWrapperImpl->cthis()->getState();
  if (state == nullptr) {
    return nullptr;
  }
  auto concreteState = std::static_pointer_cast<const ConcreteStateData>(state);
  const HybridChildrenContainerTestViewState& data = concreteState->getData();
  const std::shared_ptr<const HybridChildrenContainerTestViewProps>& props = data.getProps();
  if (props == nullptr) [[unlikely]] {
    throw std::runtime_error("HybridChildrenContainerTestViewState's data doesn't contain any props!");
  }
  return props;
}

void JHybridChildrenContainerTestViewStateUpdater::updateViewProps(jni::alias_ref<jni::JClass> /* class */,
                                           jni::alias_ref<JHybridChildrenContainerTestViewSpec::JavaPart> javaView,
                                           jni::alias_ref<JStateWrapper::javaobject> newState,
                                           jni::alias_ref<JStateWrapper::javaobject> oldState) {
  std::shared_ptr<JHybridChildrenContainerTestViewSpec> hybridView = javaView->getJHybridChildrenContainerTestViewSpec();
  std::shared_ptr<const HybridChildrenContainerTestViewProps> newProps = getPropsFromStateWrapper(newState);
  std::shared_ptr<const HybridChildrenContainerTestViewProps> oldProps = getPropsFromStateWrapper(oldState);
  if (newProps == nullptr) [[unlikely]] {
    throw std::runtime_error("Current StateWrapper doesn't contain any props!");
  }

  // Update only props that differ from the previous State snapshot.
  if (oldProps == nullptr
        ? newProps->isBlue.isProvided()
        : !newProps->isBlue.hasSameValue(oldProps->isBlue)) {
    hybridView->setIsBlue(newProps->isBlue.get());
  }

  // Update hybridRef if it changed
  if (oldProps == nullptr
        ? newProps->hybridRef.isProvided()
        : !newProps->hybridRef.hasSameValue(oldProps->hybridRef)) {
    // hybridRef changed - call it with new this
    const auto& maybeFunc = newProps->hybridRef.get();
    if (maybeFunc.has_value()) {
      maybeFunc.value()(hybridView);
    }
  }
}

} // namespace margelo::nitro::test::views
