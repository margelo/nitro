///
/// HybridChildrenTestViewSpec.kt
/// This file was generated by nitrogen. DO NOT MODIFY THIS FILE.
/// https://github.com/margelo/nitro
/// Copyright © Marc Rousavy @ Margelo
///

package com.margelo.nitro.test

import androidx.annotation.Keep
import com.facebook.jni.HybridData
import com.facebook.proguard.annotations.DoNotStrip
import dalvik.annotation.optimization.FastNative
import com.margelo.nitro.core.HybridObject
import com.margelo.nitro.views.HybridView
import android.view.ViewGroup

/**
 * A Kotlin class representing the ChildrenTestView HybridObject.
 * Implement this abstract class to create Kotlin-based instances of ChildrenTestView.
 */
@DoNotStrip
@Keep
@Suppress(
  "KotlinJniMissingFunction", "unused",
  "RedundantSuppression", "RedundantUnitReturnType", "SimpleRedundantLet",
  "LocalVariableName", "PropertyName", "PrivatePropertyName", "FunctionName"
)
abstract class HybridChildrenTestViewSpec: HybridView() {
  // Properties
  /**
   * The [ViewGroup] this HybridView is holding.
   *
   * React Native positions each child itself, so this should be a
   * [com.margelo.nitro.views.NitroViewGroup] (or another [ViewGroup] that
   * doesn't lay out its own children).
   *
   * This value should not change during the lifetime of this `HybridView`.
   */
  abstract override val view: ViewGroup
  
  /**
   * The [ViewGroup] React children are mounted into.
   *
   * Defaults to [view]. Override this when the children have to live inside a
   * sub-view - e.g. an overlay on top of a third-party [ViewGroup]. The sub-view
   * has to cover the same area as [view], otherwise React Native's layout lands
   * in the wrong place.
   */
  open val childrenContainer: ViewGroup
    get() = view
  
  @get:DoNotStrip
  @get:Keep
  @set:DoNotStrip
  @set:Keep
  abstract var isBlue: Boolean

  // Methods
  @DoNotStrip
  @Keep
  abstract fun getNativeChildCount(): Double

  // Default implementation of `HybridObject.toString()`
  override fun toString(): String {
    return "[HybridObject ChildrenTestView]"
  }

  // C++ backing class
  @DoNotStrip
  @Keep
  protected open class CxxPart(javaPart: HybridChildrenTestViewSpec): HybridObject.CxxPart(javaPart) {
    // C++ JHybridChildrenTestViewSpec::CxxPart::initHybrid(...)
    @FastNative
    external override fun initHybrid(): HybridData
  }
  override fun createCxxPart(): CxxPart {
    return CxxPart(this)
  }

  companion object {
    protected const val TAG = "HybridChildrenTestViewSpec"
  }
}
